# link-cache: investigation of where the cache underperforms (and a soundness bug)

Companion to `REPORT.md` and the bench script. Reads the bench's
per-target outcome data, classifies every MISS by root cause, and
documents two findings the bench surfaced that are worth fixing
upstream.

The bench data referenced throughout is in
`examples/link-cache-demo/.per-target.tsv` and
`examples/link-cache-demo/.bench-results.jsonl`, produced by
`scripts/link-cache-demo-bench.sh --cabal=… --runs=3 --verify`.

## TL;DR

1. **The cache delivers its designed wins.** The HIT/MISS partition
   on the 9 scenarios is *structurally predictable* from the edit's
   effect on the touched module's `.hi`. Interface-stable edits HIT
   the entire cascade; interface-breaking edits cascade-MISS through
   the importers of the touched module.
2. **The executable link's cache key is incomplete.** The manifest
   for `demo-app`'s cache entry contains exactly one input:
   `Main.hs`. None of the dep package archives are hashed in. As a
   result `demo-app` HITs in *every* scenario the bench tested,
   including ones where `demo-core.a` legitimately changed. This is
   a soundness gap — the cache serves a stale exe.
3. **GHC's `-shared` is not reproducible under partial rebuild.**
   Cold-to-cold builds are bit-identical (verified across 27
   artefacts), but the verify sweep (`CABAL_LINK_CACHE_VERIFY=1`)
   shows `HIT-DIVERGED` on `*.so` outputs even when the `.o` inputs
   match. The cache returns a *valid* linker output, just not the
   one this build's linker would have produced. Functional builds
   are fine; bit-reproducible builds are not.
4. **Line-number shifts in source bust the `.o` byte-equality.**
   `whitespace-only` and `comment-only` HIT 6/9 instead of 9/9
   because adding/removing a line shifts every subsequent line
   number, which appears in `.o` debug info, and downstream
   importers re-hash. This is a class of cache wins that's
   recoverable, with effort.

The rest of this document is the evidence.

## Per-target outcomes (before the fix in Phase 5)

Pulled from `.per-target.tsv` of an earlier bench run before the
prototype patch. A cell shows the HIT count out of 3 cache-on runs
for that (scenario, target) pair.

| scenario | core.a | core.so | codec.a | codec.so | graph.a | graph.so | engine.a | engine.so | demo-app |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| body-stable | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 |
| add-unexported | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 |
| add-exported | 0/3 | 0/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 0/3 | **3/3** |
| modify-type | 0/3 | 0/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 0/3 | **3/3** |
| refactor-internal | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | **3/3** |
| whitespace-only | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | 3/3 |
| comment-only | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | 3/3 |
| reorder-exports | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | 3/3 |
| edit-leaf-of-mid-lib | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 3/3 | **3/3** |

The boldfaced `demo-app` rows are the soundness gap: they should
MISS (deps changed) but HIT. See Finding 1 below.

## Per-target outcomes (after the Phase-5 fix)

`REPORT.md` is regenerated by every bench run; this snapshot is
the post-fix sweep at the time INVESTIGATION.md was written. The
`demo-app` column now shows `skipped` (rendered here as 0/3 HIT)
across the board — the correct outcome — and verify mode no longer
diverges on the exe.

| scenario | core.a | core.so | codec.a | codec.so | graph.a | graph.so | engine.a | engine.so | demo-app |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| body-stable | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | skip |
| add-unexported | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | skip |
| add-exported | 0/3 | 0/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 0/3 | skip |
| modify-type | 0/3 | 0/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 0/3 | skip |
| refactor-internal | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | skip |
| whitespace-only | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | skip |
| comment-only | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | skip |
| reorder-exports | 3/3 | 3/3 | 3/3 | 3/3 | 0/3 | 0/3 | 0/3 | 3/3 | skip |
| edit-leaf-of-mid-lib | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 0/3 | 3/3 | skip |

## Finding 1: exe cache key is under-keyed

**Severity:** soundness gap.

**Evidence.** A `demo-app`-targeted manifest from a populated cache:

```
tool: ghc-link-exe
target: …/dist-newstyle/.../demo-app
created: 2026-06-02 12:04:51.217308651 UTC
inputs:
  8b257fb090144279  Main.hs
```

Exactly one input: `Main.hs`. The dep archives (`libHSdemo-core.a`,
etc.) are absent from the manifest, so they are absent from the
cache key.

**Why it happens.** The exe link path is in
`Cabal/src/Distribution/Simple/GHC/Build/Link.hs:506-519`:

```haskell
mDepArchives <- linkDepArchives lbi linkOpts
let (depArchives, ctx) = case mDepArchives of
      Just paths -> (paths, baseCtx)
      Nothing    -> ([], skipReason "…" baseCtx)
withSkippableLink verbosity ctx (i target) (linkSrcs ++ depArchives)
                  (runGhcProg linkOpts{ghcOptOutputFile = toFlag target})
```

`linkSrcs` is `map i (ghcOptInputFiles linkOpts)` — for cabal v3 exe
builds in `--make` mode this is exactly `app/Main.hs`. `linkDepArchives`
is supposed to add the dep `.a`/`.so` files. The manifest shows
that for this project `linkDepArchives` is *not* contributing
anything — it returned `Just []`, the cache key was computed over
just `linkSrcs`, and the exe HITs against the baseline blob
forever.

The verify sweep catches this: with `CABAL_LINK_CACHE_VERIFY=1` the
cache re-runs the linker and byte-compares, and `demo-app` shows
`HIT-DIVERGED` on `body-stable`, `add-unexported`, `add-exported`,
`modify-type`, `refactor-internal`, `whitespace-only`,
`comment-only`, `reorder-exports`, and `edit-leaf-of-mid-lib` —
i.e. every scenario where the deps actually changed. The cached
exe is from the baseline build; the fresh linker would have produced
a different exe. The canary is doing its job — the cache key isn't.

**Hypotheses for why `linkDepArchives` returns `Just []`:**

- `ghcOptPackages linkOpts` is empty at this call site (no `-package-id`
  threaded through) — possible but contradicted by the `-v` build
  log, which shows seven `-package-id` flags actually passed to
  `ghc` for the exe link.
- The function's three search strategies (`installedPkgs lbi` lookup,
  `-L` glob, in-tree walk) all return empty *at the point the exe
  link is invoked* — the dep libs are on disk but the IPI hasn't
  been updated to include them yet, and the `-L` paths cabal passes
  point to dirs that don't actually contain the archives by that
  filename pattern. Then `foundCandidates = []`, `foundNames = []`,
  `matched _ = False` for every `expectedArchiveUidStrings` entry,
  so `allExpectedFound = False`, so the function should return
  `Nothing` and trigger `skipReason` — but the outcome reported is
  `hit`, not `skipped`. That contradicts the source. There is
  something inconsistent here that needs a debug-print to pin down.

**Cache-local fix (proposed).** Two surgical possibilities:

- **Force a `skip-reason` when `linkDepArchives` returns `Just []`
  and `ghcOptPackages` is non-empty.** Today the function trusts the
  empty list as "no deps were expected" rather than "we couldn't find
  the deps". A `case (uids, found) of ([], _) -> Just []; (_, []) ->
  Nothing` discriminator would close the gap. ~5 lines in
  `linkDepArchives`.
- **Hash the dep `.hi` ABI fingerprints into the key in place of /
  in addition to the archive bytes.** `.hi` ABI hashes are
  GHC-computed and stable; they bust the key on any visible interface
  change. Doesn't catch dep-implementation-only changes, but for an
  exe link "the deps' interface didn't change" is the relevant
  invariant — the linker's output depends on the symbols resolved,
  which is what the ABI hash captures.

The first is the minimal fix; the second is the more principled one.

**Upstream filing.** Worth a Cabal bug report.

## Finding 2: GHC `-shared` is not reproducible under incremental rebuild

**Severity:** non-deterministic but functionally benign.

**Evidence.**

- Two back-to-back **cold** builds (`rm -rf dist-newstyle && cabal build all`)
  produce byte-identical `.o`, `.a`, `.so`, and exe files across all
  27 artefacts. GHC + toolchain ARE deterministic in this setting.
- The verify sweep, which does incremental rebuilds (sync to
  baseline + apply scenario edit + link), produces `.so` files that
  diverge from the cold-populate's `.so` files at the byte level.
  Divergence in `body-stable`'s `demo-core.so`: same total size
  (96472 bytes), 14742 differing bytes, first divergence at offset
  209 (inside a program header's `p_filesz`), then scattered
  through `.text`, `.dynstr`, `.rodata`, and `.symtab` sections.
- The cached blob is a *valid* `.so` (correct symbols, runs as
  expected). It's just not the same bytes the linker would produce
  *right now*.

**Why it likely happens.** Something about the incremental-rebuild
state of GHC's compilation produces `.o` files that are byte-equal
to the cold-build `.o`s for the same source under `-O1` (we
confirmed `ar`-static of `demo-core.a` `HIT-VERIFIES` cleanly) but
the subsequent `ghc -shared` invocation lays them out differently —
likely an artefact of how GHC drives `gold` (the system linker is
`gold 1.16` on this host, embedded in a `.note.gnu.gold-version`
section). Section ordering inside the `.so`, dynamic-symbol-table
ordering, or alignment-padding decisions in gold can vary
run-to-run without affecting program semantics.

**Cache-local fix (proposed).**

- **Strip non-deterministic sections before computing the cache
  key.** Run a normalising pass over `.so` inputs/outputs (drop
  `.note.gnu.*`, sort `.dynsym`, zero out alignment padding) and
  hash the canonical form. The on-disk `.so` keeps its original
  bytes; only the cache key is over the canonical form. This is
  invasive — it requires a small ELF reader.
- **Tag known-non-deterministic tools and downgrade verify
  divergence to a notice for them.** Currently
  `CABAL_LINK_CACHE_VERIFY_FAIL=1` escalates *any* divergence to a
  hard error, which is too strict for `ghc-shared` in the wild. A
  `ghc-shared` divergence is a known-acceptable canary noise; a
  `ar-static` divergence is a real soundness bug. Different tools
  should have different verify policies.

**Upstream filing.** This is also worth raising with GHC: pin down
which `ld`/`gold` flags or GHC linker shims are responsible, then
either fix or document.

## Finding 3: line-number shifts cost downstream HITs

**Severity:** wins-on-the-table, no soundness issue.

**Evidence.** `whitespace-only`, `comment-only`, and
`reorder-exports` all show 6/9 HITs, the same partition as
`refactor-internal`: `graph.{a,so}` and `engine.a` MISS. The
expectation was 9/9 for these (lexer drops whitespace and comments,
GHC canonicalises export order).

The MISSes happen because GHC's `.o` files embed source line
numbers (and possibly source file fingerprints) for use in error
messages and `HasCallStack`. A blank line inserted in `Util.hs`
shifts every subsequent line number; `Util.o`'s embedded
line-number table changes; `Util`'s `.hi` references those line
numbers in its source-location metadata; downstream importers
re-hash; their `.o` files come out byte-different; their `.a`
keys change; cache MISSes.

This is consistent with the surprise that `demo-codec.{a,so}` HITs
in these scenarios (it doesn't import `Util`) but `demo-graph.a/so`
and `demo-engine.a` don't (they do). The cache is faithfully
tracking real `.o` bytes; the bytes are just sensitive to
position-information changes that shouldn't matter semantically.

**Cache-local fix (proposed).**

- **Hash `.o` files canonically:** strip the line-number /
  debug-info / source-file-fingerprint sections before computing
  the input digest. As with Finding 2's `-shared` proposal, this
  requires an ELF parser but is the lasting fix that would
  generalise across edit shapes that are "no-op + line shift".
- **Cheaper alternative:** include `.hi` ABI fingerprints alongside
  `.o` digests, weighted to dominate when the `.hi` is byte-stable.
  This wouldn't actually flip the bench's MISSes (the `.o` files
  really do differ), but it would let downstream consumers decide
  to bypass relink when interface stability is provable.

**Asymmetry note.** Several scenarios show `engine.a` MISS but
`engine.so` HIT (`refactor-internal`, `whitespace-only`,
`comment-only`, `reorder-exports`). The static archive uses `.o`
files; the shared library uses `.dyn_o` files (separately
compiled). The asymmetry says that `.dyn_o` for these scenarios is
byte-stable but `.o` is not. Worth pinning down whether GHC's
dynamic-way codegen differs from static-way codegen in how it
embeds source positions.

## Finding 4: structural partition (the good news)

For completeness, the partition that *does* match prediction:

- `body-stable` and `add-unexported` HIT 9/9. GHC's `-O1`
  dead-code-eliminates a no-op `let` wrapper and any unused
  underscore-prefixed binding; the `.o` files are byte-equal to
  baseline; every link in the cascade HITs.
- `refactor-internal`, `whitespace-only`, `comment-only`, and
  `reorder-exports` HIT 6/9 — exactly the prediction for an
  interface-stable edit that perturbs `Util.o` bytes:
  `demo-codec.{a,so}` HIT (no `Util` import), the deeper libs MISS.
- `add-exported` and `modify-type` HIT 3/9 — exactly the prediction
  for an ABI-changing edit: only `demo-codec.{a,so}` (which doesn't
  import `Util`) HIT.
- `edit-leaf-of-mid-lib` HITs only `engine.so` and `demo-app`. The
  prediction was that `demo-core` and `demo-graph` would HIT (they
  don't depend on `demo-codec`), but they don't. This is likely a
  bench artefact: the test sequence's incremental rebuild leaves
  `demo-core` and `demo-graph`'s `.o` files in a state that doesn't
  exactly match the cold-populate, *or* the dep walks pull
  `Frame.hs` into more `.hi` graphs than the source dependency
  suggests. Worth investigating.

## Classification of every MISS

A = inputs differ genuinely (cache cannot help under byte-exact model)
B = ghc-recompile-conservative (downstream `.o` differs without semantic justification — upstream GHC fix)
C = cabal-relink-eager / cache-key-incomplete (the cache could close this gap)
D = key-relaxable (cache key contains something soundness doesn't require)

| scenario | target | outcome | class |
|---|---|---|---|
| body-stable, add-unexported | all 9 | HIT | – |
| add-exported | core.{a,so}, graph.{a,so}, engine.{a,so} | MISS | A (`.o` of touched module + `.hi` of touched module both change; downstream forced recompile by GHC is correct) |
| add-exported | demo-app | *spurious HIT* | **C** — exe key missing dep archives (Finding 1) |
| modify-type | core.{a,so}, graph.{a,so}, engine.{a,so} | MISS | A |
| modify-type | demo-app | *spurious HIT* | **C** (Finding 1) |
| refactor-internal | core.{a,so}, exe | MISS | A (`.o` of touched module differs; exe key correctly includes dep archive bytes — well, it doesn't, but if it did, this MISS would be correct) |
| refactor-internal | graph.{a,so}, engine.a | MISS | **B/D** — `.hi` of `Util` is stable but downstream `.o` differs because GHC re-emits source-position-dependent data |
| whitespace-only, comment-only, reorder-exports | graph.{a,so}, engine.a | MISS | **D** — line shift propagates into `.o` debug info (Finding 3) |
| edit-leaf-of-mid-lib | core.{a,so}, codec.{a,so}, graph.{a,so}, engine.a | MISS | A for `codec.*` + `engine.a` (real cascade); B/incremental-rebuild artefact for the others (need more investigation) |

## Prototype candidates, prioritised

Ranked by expected impact in this demo. All cache-local unless noted.

1. **Force-skip the exe cache when the link is in `--make` mode.**
   ✅ **Prototyped, measured, and committed in this branch.** Patch
   in `Cabal/src/Distribution/Simple/GHC/Build/Link.hs` (around
   line 515): after computing `mDepArchives`, look at `linkSrcs`;
   if any input ends in `.hs`/`.lhs` *and* `mDepArchives` came back
   as `Just []`, treat the cache as untrustworthy for this exe link
   and attach a `skipReason`. The exe outcome then surfaces as
   `skipped` (which is correctly excluded from HIT counts) rather
   than a spurious `hit`.

   **Measured effect** (full sweep, 3 runs):

   - Before the fix, every scenario showed `demo-app` HITting,
     even `add-exported` and `modify-type` where the exe could
     not possibly be the same binary.
   - After the fix, `demo-app` shows `skipped` for every scenario
     in `.per-target.tsv`.
   - Verify-mode divergence counts dropped from 3→2 per scenario
     in interface-stable cases (the exe is no longer in the
     diverging set), and `add-exported`/`modify-type` now pass the
     verify sweep cleanly (`PASS (verified=0 misses=8)`) instead of
     diverging on the exe.

   The headline HIT totals appear lower because what was a false
   HIT is now an honest skip; the per-target table reads cleaner.
2. **Per-tool verify policy.** Add a configuration knob (env or
   `Verify` field) so that `ghc-shared` divergences log a warning
   while `ar-static` divergences hard-fail. Closes the false-positive
   noise from Finding 2.
3. **Canonical `.o` hashing.** Add a `hashInputCanonical` variant
   that strips `.debug_*` and `.note.gnu.*` sections before xxh64.
   Closes Finding 3 and helps Finding 2's downstream pattern. Cost:
   adding a small ELF reader (or shelling out to `objcopy
   --strip-debug --strip-unneeded`).
4. **Persistent stat-index across cabal-install invocations.** Save
   the index to `$CABAL_LINK_CACHE_DIR/.stat-index` on exit, load on
   start, invalidate stale entries. Doesn't change HIT rate; reduces
   per-build hashing time when the cache is large. Useful in CI.

## Out of scope but worth filing upstream

- **GHC**: investigate whether `.dyn_o` and `.o` codegen embed source
  position info differently (we observed `engine.so` HIT vs `engine.a`
  MISS for line-shift edits — Finding 3).
- **GHC + `gold`**: pin down which `.so` linker step is
  non-reproducible (Finding 2). Candidate sources: section
  ordering, `.dynsym` ordering, alignment padding.
- **Cabal**: re-document `Note [Link manifest cache]` once the
  `linkDepArchives` semantics in Finding 1 are nailed down — the
  current note is correct in intent but the code doesn't realise
  it.

## How to reproduce these findings

1. `bash scripts/link-cache-demo-bench.sh --cabal=/path/to/result-opt/bin/cabal --runs=3 --verify` — re-runs the full sweep with the diagnostic layer.
2. `cat examples/link-cache-demo/.per-target.tsv | awk -F'\t' '$3 == "demo-app"'` — confirms the exe HITs in every scenario (Finding 1).
3. Inspect any `demo-app`-targeted manifest in `<work>/cache/<scen>/`:
   `cat <work>/cache/body-stable/$(grep -l 'target:.*demo-app$' <work>/cache/body-stable/*.manifest)`. Only `Main.hs` will be listed (Finding 1).
4. `scripts/link-cache-manifest-diff.sh A.manifest B.manifest` — diff two manifests for the same target; pinpoints which input digest changed and by how much.
5. For a Finding-2 dump: when verify fails, the cache directory has a `divergences/<key>/` subdirectory with `blob` (cached) and `linker` (just produced). `cmp -l blob linker | wc -l` gives the byte delta.
